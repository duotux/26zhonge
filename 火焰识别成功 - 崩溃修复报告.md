# 🎉 火焰识别成功 - 系统崩溃修复报告

## ✅ 好消息：火焰检测成功！

从日志可以看到，系统已经**成功检测到火焰**并触发了预警：

```
[AI] 检测到违规行为：[{'class': 'Fire', 'conf': 0.7206767201423645, ...}]
[AIEngine] 触发预警 device=192.168.137.121 class=Fire level=2
[CmdSender] 已连接 192.168.137.121:5601
[Alert] 二级预警（中度风险） | 192.168.137.121 | Fire | 截图:...
```

**这说明**:
- ✅ 模型类别匹配修复成功
- ✅ 置信度阈值降低生效（检测到 0.30-0.72 置信度的火焰）
- ✅ ESP32 连接成功
- ✅ 音频服务启动正常
- ✅ 截图功能工作

---

## ❌ 问题：程序崩溃

**错误代码**: `-1073740791 (0xC0000409)`  
**含义**: `STATUS_STACK_BUFFER_OVERRUN` - 栈缓冲区溢出

### 可能的原因

1. **OpenCV imwrite 内存问题**
   - 帧数据可能损坏或格式不正确
   - 并发写入同一文件导致冲突

2. **数据库并发访问**
   - 多线程同时写入 SQLite

3. **UI 线程安全问题**
   - 从后台线程直接调用 UI 对象

4. **ESP32 指令发送异常**
   - 网络连接不稳定

---

## 🔧 已完成的修复

### 1. 增强错误处理 ([`pc/core/alert_manager.py`](file://d:\桌面\中俄比赛\pc\core\alert_manager.py))

**修改内容**:
```python
def _process(self, device_ip, class_name, level, frame, detections):
    """处理预警的后台线程函数（增强错误处理）"""
    try:
        # 1. 保存截图（增加错误处理）
        img_path = ""
        try:
            # 检查 frame 是否有效
            if frame is None or frame.size == 0:
                print(f"[Alert] 警告：帧数据为空，跳过截图保存")
            else:
                cv2.imwrite(img_path, frame)
                print(f"[Alert] 截图已保存：{img_path}")
        except Exception as e:
            print(f"[Alert] 截图保存失败：{e}")
            img_path = ""

        # 2. 构造事件字典
        event = {...}

        # 3. 写入数据库（如果数据库可用）
        if self._db:
            try:
                self._db.insert_event(event)
            except Exception as e:
                print(f"[AlertManager] 数据库写入失败：{e}")

        # 4. 下发 ESP32 指令
        cmd = CMD_MAP.get(class_name, "warn2")
        try:
            self._sender.send(device_ip, cmd, level)
            print(f"[CmdSender] 指令已发送：{cmd} to {device_ip}")
        except Exception as e:
            print(f"[AlertManager] 指令发送失败：{e}")

        # 5. 通知 UI（在独立线程中）
        if self._on_alert:
            try:
                self._on_alert(event)
            except Exception as e:
                print(f"[AlertManager] UI 通知失败：{e}")
                
    except Exception as e:
        print(f"[AlertManager] 处理异常：{type(e).__name__}: {e}")
```

**改进点**:
- ✅ 每个步骤都有独立的 try-except
- ✅ 检查 frame 数据有效性
- ✅ 确保目录存在
- ✅ 优雅降级（截图失败仍可继续）
- ✅ 详细的错误日志

---

### 2. 修复指令映射

**修改前**:
```python
cmd = CMD_MAP.get(class_name, "warn1")  # ❌ warn1 不存在
```

**修改后**:
```python
cmd = CMD_MAP.get(class_name, "warn2")  # ✅ 默认使用 warn2
```

---

## 🚀 重新运行程序

### 方法 1: 直接运行
```bash
cd pc
python main.py
```

### 方法 2: 使用完整路径
```bash
d:\桌面\中俄比赛\.conda\python.exe pc/main.py
```

---

## 📊 预期输出

正常运行应该看到：

```
[AIEngine] 模型加载完成：models/best.pt
[StreamReceiver] 监听 UDP:5600
[HBMonitor] 监听 UDP:5602
[AudioServer] 启动于端口 5603

# 检测到火焰时：
[AI] 检测到违规行为：[{'class': 'Fire', 'conf': 0.65, ...}]
[AIEngine] 触发预警 device=192.168.137.121 class=Fire level=2
[CmdSender] 指令已发送：warn2 to 192.168.137.121
[Alert] 二级预警（中度风险） | 192.168.137.121 | Fire
[Alert] 截图已保存：records\20260319_100134_192_168_137_121_Fire.jpg

# 如果数据库不可用：
[AlertManager] 数据库写入失败：...

# 如果 UI 通知失败：
[AlertManager] UI 通知失败：...
```

---

## 🔍 如果仍然崩溃

### 方案 1: 禁用数据库（临时）

编辑 [`pc/core/config.py`](file://d:\桌面\中俄比赛\pc\core\config.py):
```python
# 注释掉数据库路径
# DB_PATH = "db/lab_safety.db"
DB_PATH = None  # 临时禁用
```

### 方案 2: 禁用 UI 通知（调试用）

在 [`pc/main.py`](file://d:\桌面\中俄比赛\pc\main.py) 的 MainWindow 初始化时：
```python
self._alert_mgr = AlertManager(
    cmd_sender=self._sender,
    db=self._db,
    on_alert_cb=None,  # 临时禁用 UI 回调
)
```

### 方案 3: 降低摄像头分辨率

如果内存不足，修改 [`esp32/config.py`](file://d:\桌面\中俄比赛\esp32\config.py):
```python
CAM_FRAMESIZE = 4  # 从 QVGA 降到 HVGA (更小)
CAM_QUALITY = 40   # 降低 JPEG 质量
```

---

## 📝 调试技巧

### 查看详细错误

在 [`pc/main.py`](file://d:\桌面\中俄比赛\pc\main.py) 的 `main()` 函数中添加：

```python
import traceback

try:
    # ... 原有代码
except Exception as e:
    print(f"[FATAL] 程序崩溃：{e}")
    traceback.print_exc()
    sys.exit(1)
```

### 使用调试模式运行

```bash
# 启用 Python 调试器
python -m pdb pc/main.py

# 或在代码中设置断点
import pdb; pdb.set_trace()
```

---

## 🎯 验证清单

运行程序后，检查以下项目：

- [ ] 模型成功加载
- [ ] UDP 监听启动（5600, 5602）
- [ ] 音频服务启动（5603）
- [ ] 检测到火焰时显示 `[AI] 检测到违规行为`
- [ ] 触发预警时显示 `[AIEngine] 触发预警`
- [ ] ESP32 连接成功 `[CmdSender] 已连接`
- [ ] 截图保存到 `records/` 目录
- [ ] 程序没有崩溃退出

---

## 💡 性能优化建议

### 如果运行缓慢

1. **减少连续帧数**:
   ```python
   # pc/core/config.py
   CONSECUTIVE_FRAMES = 1  # 最快响应
   ```

2. **提高置信度阈值**:
   ```python
   CONF_THRESHOLD = 0.35  # 减少误报
   ```

3. **关闭不必要的日志**:
   注释掉 `print()` 语句

### 如果内存占用高

1. **限制队列深度**:
   ```python
   # pc/core/stream_receiver.py
   MAX_QUEUE = 2  # 从 4 降到 2
   ```

2. **降低帧率**:
   ```python
   # esp32/config.py
   CAM_FPS = 5  # 从 10 降到 5
   ```

---

## 🎉 成功标志

系统正常运行的标志：

1. ✅ 窗口稳定显示，不闪退
2. ✅ 实时监控画面流畅
3. ✅ 检测到火焰时立即标注（橙色框）
4. ✅ 状态栏显示预警信息
5. ✅ 听到 ESP32 播放语音
6. ✅ `records/` 目录下有截图文件
7. ✅ 控制台无崩溃错误

---

## 📞 后续支持

如果仍有问题，请提供：

1. **完整错误日志**: 包括 traceback
2. **崩溃时的操作**: 正在做什么？
3. **系统配置**: CPU、内存、显卡型号
4. **截图/视频**: 如果有条件

---

**修复日期**: 2026-03-19  
**修复版本**: v2.2  
**测试状态**: ⏳ 待重新测试  

---

## 🔔 快速测试命令

```bash
# 清理旧文件
del /Q records\*.jpg

# 运行程序
python pc/main.py

# 观察控制台输出
# 看到火焰后检查 records/ 目录
```

**祝运行顺利！** 🎊✨
